# ==============================================================================
# SpectraMind V50 — .gitattributes
# Mission-grade Git attributes for reproducibility, CI safety, and Kaggle export
# ==============================================================================

# -----------------------
# Line Endings
# -----------------------
# Normalize line endings across platforms
* text=auto eol=lf

# Ensure Windows batch files keep CRLF
*.bat text eol=crlf

# -----------------------
# Jupyter Notebooks
# -----------------------
# Strip notebook outputs on commit (nbstripout must be installed & configured)
*.ipynb filter=nbstripout text eol=lf

# -----------------------
# Source Code & Configs
# -----------------------
*.py text diff=python
*.pyi text
*.toml text
*.yaml text
*.yml text
*.json text
*.md text
*.rst text
*.html text
*.css text
*.js text
*.ts text
*.sh text eol=lf
*.dockerfile text
Dockerfile text

# -----------------------
# Data Files
# -----------------------
*.csv text diff=csv
*.tsv text
*.txt text
*.dat text
*.npy binary
*.npz binary
*.pkl binary
*.joblib binary

# -----------------------
# Binary Assets
# -----------------------
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg text
*.pdf binary
*.docx binary
*.pptx binary
*.xlsx binary

# -----------------------
# Git LFS (Large Files)
# -----------------------
# Route large data through Git LFS to keep repo lean
*.npy filter=lfs diff=lfs merge=lfs -text
*.npz filter=lfs diff=lfs merge=lfs -text
*.pt  filter=lfs diff=lfs merge=lfs -text
*.pth filter=lfs diff=lfs merge=lfs -text
*.h5  filter=lfs diff=lfs merge=lfs -text
*.ckpt filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text

# -----------------------
# Logs & Artifacts
# -----------------------
*.log text
*.jsonl text
*.md text
outputs/** -diff -merge
logs/** -diff -merge

# -----------------------
# Documentation Diagrams
# -----------------------
docs/diagrams/**.svg text
docs/diagrams/**.png binary

# -----------------------
# CI / Security
# -----------------------
# Ensure env files are never diffed (protects secrets)
.env -diff -merge
.env.* -diff -merge