# ==============================================================================
# SpectraMind V50 — Requirements (Neuro-Symbolic, Physics-Informed AI Pipeline)
# Ensures reproducibility, Kaggle compliance, Hydra-safe configs, and diagnostics.
# ==============================================================================

# ----- Core CLI & Config -----
typer==0.12.3
rich==13.7.1
hydra-core==1.3.2
omegaconf==2.3.0
pyyaml==6.0.1
pydantic==2.7.1
python-dotenv==1.0.1
packaging==24.1
click==8.1.7
jinja2==3.1.4

# ----- Scientific Python Stack -----
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
scikit-learn==1.4.2
joblib==1.4.2
tqdm==4.66.4
psutil==5.9.8

# ----- PyTorch + Geometric -----
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
torch-geometric==2.5.3 ; platform_system != "Windows"
networkx==3.2.1

# ----- Visualization / Diagnostics -----
matplotlib==3.8.4
plotly==5.22.0
umap-learn==0.5.6
shap==0.45.0
seaborn==0.13.2          # for quick diagnostics heatmaps
altair==5.3.0            # lightweight Vega-lite plotting
ipywidgets==8.1.2        # Jupyter/HTML interactivity for diagnostics

# ----- Neuro-Symbolic / Logic -----
sympy==1.13.0            # symbolic math for physics constraints
networkx==3.2.1
graphviz==0.20.3         # logic graphs, diagnostics overlays

# ----- Astrophysics / Modeling -----
astropy==6.1.1           # astronomy/astrophysics utilities
h5py==3.11.0             # large scientific dataset handling
numba==0.60.0            # fast JIT for physics kernels
xarray==2024.6.0         # labeled arrays (time/wavelength cubes)

# ----- Experiment Tracking / Data -----
dvc==3.51.2
mlflow==2.14.3
wandb==0.17.6
lakefs==0.109.0          # optional: versioning for large data lakes

# ----- Kaggle Integration -----
kaggle==1.6.17           # Kaggle API (submit, dataset, kernels)
cloudpickle==3.0.0       # serialization for Kaggle jobs

# ----- Utilities -----
requests==2.32.3
urllib3==2.2.2
fsspec==2024.6.0
pyarrow==17.0.0          # parquet/feather handling
fastapi==0.112.2         # optional dashboard backend
uvicorn==0.30.5          # fastapi server runtime

# ----- Dev / CI / Lint -----
black==24.8.0
isort==5.13.2
ruff==0.6.9
mypy==1.11.2
pre-commit==3.8.0
pytest==8.3.2
pytest-cov==5.0.0

# ----- Jupyter / Docs -----
jupyterlab==4.2.4
notebook==7.2.2
mkdocs==1.6.1
mkdocs-material==9.5.30

# ==============================================================================
# Notes:
# • torch-geometric excluded on Windows due to wheel incompatibility
# • pinned versions ensure Kaggle/CI reproducibility
# • astrophysics libs (astropy, xarray, numba) aid physics-informed modeling
# • diagnostics stack: matplotlib + plotly + seaborn + shap + altair + ipywidgets
# • dev/ci tools included for GitHub Actions + pre-commit
# • safe for Kaggle runtime (no system-breaking deps)
# ==============================================================================