# ==============================================================================

# SpectraMind V50 — requirements.txt  (Neuro‑Symbolic, Physics‑Informed AI Pipeline)

# - Pinned, Kaggle‑safe, Hydra‑first, CLI‑driven stack

# - CPU/GPU friendly; PyG excluded on Windows due to wheel availability

# - Organized by subsystem; duplicates removed; minor version bumps for stability

# References: SpectraMind V50 plan (Typer/Hydra/DVC/CI) , Hydra usage , Ubuntu CLI workflow

# ==============================================================================

# ----- Core CLI & Config -----

typer==0.12.3
rich==13.7.1
hydra-core==1.3.2
omegaconf==2.3.0
pyyaml==6.0.2
pydantic==2.8.2
python-dotenv==1.0.1
packaging==24.1
click==8.1.7
jinja2==3.1.4

# ----- Scientific Python Stack -----

numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
scikit-learn==1.4.2
joblib==1.4.2
tqdm==4.66.4
psutil==5.9.8

# ----- PyTorch + (optional) Geometric -----

# NOTE: Pin to CUDA wheels externally if needed (e.g. Kaggle uses preinstalled torch).

torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# Exclude torch-geometric on Windows (no official wheels). Use on Linux/macOS only.

torch-geometric==2.5.3 ; platform\_system != "Windows"

# Graph utils

networkx==3.2.1

# ----- Visualization / Diagnostics -----

matplotlib==3.8.4
plotly==5.22.0
umap-learn==0.5.6
shap==0.45.0
seaborn==0.13.2
altair==5.3.0
ipywidgets==8.1.2

# ----- Neuro‑Symbolic / Logic -----

sympy==1.13.0
graphviz==0.20.3

# ----- Astrophysics / Modeling -----

astropy==6.1.1
h5py==3.11.0
numba==0.60.0
xarray==2024.6.0

# ----- Experiment Tracking / Data -----

dvc==3.51.2
mlflow==2.14.3
wandb==0.17.6

# lakeFS Python client (name differs from service); keep if used in your pipeline:

lakefs-client==0.127.0

# ----- Kaggle Integration -----

kaggle==1.6.17
cloudpickle==3.0.0

# ----- Utilities -----

requests==2.32.3
urllib3==2.5.0
fsspec==2024.6.0
pyarrow==17.0.0
fastapi==0.112.2
uvicorn==0.30.5

# ----- Dev / CI / Lint -----

black==24.8.0
isort==5.13.2
ruff==0.6.9
mypy==1.11.2
pre-commit==3.8.0
pytest==8.3.2
pytest-cov==5.0.0

# ----- Jupyter / Docs -----

jupyterlab==4.2.4
notebook==7.2.2
mkdocs==1.6.1
mkdocs-material==9.5.30

# ==============================================================================

# Notes:

# • Unified Typer CLI + Hydra composition across pipeline stages for reproducibility.

# • Config‑as‑code; CI snapshots + DVC data tracking; auditability for runs.

# • Terminal‑first, scriptable workflow aligns with Ubuntu CLI blueprint.

# • torch‑geometric excluded on Windows due to wheel availability; install manually if needed.

# • For CUDA‑specific torch wheels, use PyTorch index flags during install (outside this file).

# ==============================================================================
