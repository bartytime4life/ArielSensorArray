# ==============================================================================
# SpectraMind V50 — DVC Git Ignore Rules (Upgraded)
# ------------------------------------------------------------------------------
# Purpose:
#   • Prevent committing heavy or environment-specific DVC internals
#   • Keep Git history reproducible while allowing DVC to manage large artifacts
#   • CI-, Kaggle-, and Docker-safe defaults
#   • Complements .gitignore and .dvcignore
# ==============================================================================

# ───────────────────────── DVC Core Internals ─────────────────────────
.dvc/cache/              # Local cache of blobs (large, not for Git)
.dvc/tmp/                # Ephemeral temp workspace (locks, stage runs, exp cache)
.dvc/lock/               # Lock files (pipeline runtime state)
.dvc/state/              # SQLite state DB (dynamic)
.dvc/experiments/        # Experiment DAGs, not reproducible
.dvc/exp/                # Experimental scratch metadata
.dvc/logs/               # Local-only debug logs
.dvc/events/             # Local-only event/trace logs

# ───────────────────────── Local Overrides ─────────────────────────
.dvc/config.local        # Developer-specific secrets / local remote config
.dvc/user/               # User/session scratch dirs
.dvc/remotes/local/      # Local remotes not meant for Git

# ───────────────────────── Experiment / Run Artifacts ─────────────────────────
.dvc/plots/tmp/          # Interactive plot caches
.dvc/stage.lock          # Auto-generated stage lock (not portable)

# ───────────────────────── Optional / Transient ─────────────────────────
# These may appear depending on CLI runs or extensions
.dvc/.cache/             # Nested cache dirs if plugins/tools use them
.dvc/tmp.*               # Misc ephemeral lock/temp files

# ================================
# SAFE TO COMMIT (KEEP IN GIT):
#   ✔ .dvc/config
#   ✔ .dvcignore
#   ✔ dvc.yaml
#   ✔ *.dvc pointer files
#   ✔ .dvc/plots/* (templates, tracked plots)
# ================================

# ==============================================================================
# Notes:
# • This file keeps DVC’s internal runtime noise out of Git.
# • All scientific artifacts (data/, outputs/, models/, artifacts/) remain tracked by DVC.
# • Only reproducible pointers and pipeline descriptors are versioned in Git.
# ==============================================================================
