# ==============================================================================
# SpectraMind V50 — DVC Git Ignore Rules
# ------------------------------------------------------------------------------
# Purpose:
#   • Prevents committing heavy or environment-specific DVC internals
#   • Ensures reproducibility while keeping Git clean
#   • Safe defaults for CI, local dev, and Kaggle sync
# ==============================================================================

# ───────────────────────── DVC Core Internals ─────────────────────────
.dvc/cache/              # Local cache (large blobs, copy-on-write)
.dvc/tmp/                # Ephemeral tmp workspace (locks, stage runs, exp cache)

# ───────────────────────── Local Overrides ─────────────────────────
.dvc/config.local         # Developer-specific overrides (secrets, local remotes)

# ───────────────────────── Experiment / Run Artifacts ─────────────────────────
.dvc/exp/                # Local experiment tracking metadata
.dvc/experiments/        # Temporary exp DAGs
.dvc/lock/               # Lock files (not needed in Git history)
.dvc/state/              # SQLite state DB, updated dynamically
.dvc/stage.lock          # Stage runtime locks (machine-local only)

# ───────────────────────── Logs & Debug ─────────────────────────
.dvc/logs/               # Local-only logs (not reproducible)
.dvc/events/             # JSONL/trace logs for interactive runs

# ───────────────────────── Optional Extensions ─────────────────────────
.dvc/remotes/local/      # Local remote mirrors
.dvc/plots/tmp/          # Interactive plot cache
.dvc/user/               # User/session scratch (CLI runs, previews)

# ───────────────────────── Safety Notes ─────────────────────────
# SAFE TO COMMIT (KEEP IN GIT):
#   • .dvc/config (shared remote definitions, safe defaults)
#   • .dvcignore
#   • dvc.yaml (pipeline stages + DAG)
#   • *.dvc pointer files
#   • .dvc/plots/* (templates and specs tracked in repo)
#
# NEVER COMMIT:
#   • Cache/state/tmp/lock dirs above (machine-local, non-reproducible)
#   • Any secrets in .dvc/config.local
# ==============================================================================

