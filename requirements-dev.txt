# ======================================================================
# SpectraMind V50 — requirements-dev.txt  (2025-08)
# ----------------------------------------------------------------------
# Purpose: Developer tooling for linting, testing, typing, hooks, and reports.
# Usage:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pre-commit install
#   pytest -q
# ======================================================================

# ----------------------------
# Linters / Formatters
# ----------------------------
ruff==0.6.9
black==24.8.0
isort==5.13.2

# Lint notebooks with your existing tools (ruff/black/isort) via nbqa
nbqa==1.8.5

# ----------------------------
# Testing / Coverage
# ----------------------------
pytest==8.2.0
pytest-cov==5.0.0
pytest-xdist==3.6.1        # parallel tests: pytest -n auto
hypothesis==6.106.1        # property-based tests
coverage[toml]==7.6.1      # reads pyproject.toml if you configure there

# Robust CI test runs (timeouts/flaky tests/perf)
pytest-timeout==2.3.1      # fail-fast on hung tests in CI
pytest-rerunfailures==13.0 # auto-rerun flaky tests (network/seed)
pytest-benchmark==4.0.0    # track perf regressions on hot paths

# Mocking & time/HTTP helpers (useful across the pipeline)
pytest-mock==3.14.0
freezegun==1.5.1
responses==0.25.3          # mock requests
# httpx==0.27.0            # uncomment if you test httpx clients

# ----------------------------
# Pre-commit hooks
# ----------------------------
pre-commit==3.7.1

# ----------------------------
# Typing / Stubs
# ----------------------------
mypy==1.10.0
typing-extensions==4.12.2
mypy-extensions==1.0.0
types-PyYAML==6.0.12.20240808
types-requests==2.32.0.20240712

# ----------------------------
# Security / Supply-chain (optional but recommended in CI)
# ----------------------------
# pip-audit==2.7.3         # scan installed packages for known CVEs
# safety==3.2.6            # alternative CVE scanner

# ----------------------------
# Release / env tooling (optional)
# ----------------------------
# pip-tools==7.4.1         # compile lockfiles if you ever need pure-pip locking
# nox==2024.4.15           # task runner for multi-env test matrices

# ----------------------------
# Optional docs (local) — enables `mkdocs serve` with Mermaid support
# ----------------------------
# mkdocs==1.6.1
# mkdocs-material==9.5.32
# mkdocstrings[python]==0.25.1
# mkdocs-mermaid2-plugin==1.1.1

# ----------------------------
# Optional Markdown tooling (local)
# ----------------------------
# mdformat==0.7.17
# mdformat-gfm==0.3.5
# mdformat-black==0.1.1

# ----------------------------
# Optional richer terminal UX in notebooks
# ----------------------------
# rich[jupyter]==13.7.1